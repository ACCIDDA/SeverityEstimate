# Print Method for `SummaryEstimateModel` Objects

Prints a `SummaryEstimateModel` object in a structured format.

## Usage

``` r
# S3 method for class 'SummaryEstimateModel'
print(x, digits = max(3L, getOption("digits") - 3L), ...)
```

## Arguments

- x:

  An object of class `SummaryEstimateModel`.

- digits:

  The number of significant digits to print for prior parameters.

- ...:

  Unused.

## Value

`x` invisibly.
