# Find And Render A Template

Find And Render A Template

## Usage

``` r
render_template(template, template_data = list())
```

## Arguments

- template:

  The name of the template to render, should match a file under the
  `inst/` directory.

- template_data:

  The data to use when rendering the template.

## Value

A rendered template as a single length character.
