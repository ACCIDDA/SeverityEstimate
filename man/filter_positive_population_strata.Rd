% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-helpers.R
\name{filter_positive_population_strata}
\alias{filter_positive_population_strata}
\alias{strata_key}
\alias{format_strata_values}
\title{Prepare Strata Inputs For Model Fitting}
\usage{
filter_positive_population_strata(
  linelist,
  population,
  strata_cols,
  population_value,
  strata_reference
)

strata_key(x, strata_cols)

format_strata_values(x)
}
\arguments{
\item{linelist}{A \code{data.frame} containing the observed line-list records.
It must contain all columns named in \code{strata_cols}.}

\item{population}{A \code{data.frame} containing population counts by strata. It
must contain all columns named in \code{strata_cols} and the column named by
\code{population_value}.}

\item{strata_cols}{A character vector naming the strata columns shared by
\code{linelist}, \code{population}, and \code{strata_reference}.}

\item{population_value}{A single string naming the population count column in
\code{population}.}

\item{strata_reference}{A \code{data.frame} containing the ordered strata cells
that should be available to \code{\link[=incidence_population_arrays]{incidence_population_arrays()}}. For configured
strata this is usually the full cross-product of user-specified strata
levels; for the no-strata case it contains the synthetic \code{.strata} cell.}

\item{x}{A \code{data.frame} containing strata columns.}
}
\value{
\code{filter_positive_population_strata()} returns a named list with:
\itemize{
\item \code{population}: \code{population} restricted to rows whose aggregate strata
population is positive.
\item \code{strata_reference}: \code{strata_reference} restricted to the same positive
strata cells, with its input row order preserved.
}

\code{strata_key()} returns a character vector with one key per row in \code{x}.

\code{format_strata_values()} returns a single diagnostic string describing the
strata values in \code{x}.
}
\description{
Internal helpers used by \code{\link[=fit]{fit()}} before incidence and population arrays are
built. The Stan severity model requires every modeled strata cell to have a
positive denominator because the community hazard prior is centered on
observed passive incidence divided by the strata population. Strata cells
with zero population therefore cannot be passed through as ordinary modeled
cells.

\code{filter_positive_population_strata()} removes zero-population strata from
the population data and from the explicit strata reference grid used to shape
the fitted arrays. Population rows are first aggregated over \code{strata_cols},
so duplicated population rows are treated as a single strata cell and are
retained when their total population is positive. If no strata cell has a
positive population, the function errors because there is no valid fitted
model to construct.

Line-list observations are not silently dropped. After identifying positive
population strata, the function checks that every observation belongs to a
retained strata cell. Observations in a zero-population cell, or in a cell
missing from the positive population strata, produce an early R error with
the offending strata values. This keeps impossible inputs out of Stan while
preserving the original incidence accounting.

\code{strata_key()} creates stable row keys for comparing strata cells across
data frames. It is used only for exact equality checks within already
validated strata columns.

\code{format_strata_values()} formats one or more strata rows for diagnostics.
}
\keyword{internal}
