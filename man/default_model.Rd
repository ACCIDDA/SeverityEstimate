% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/default_model.R
\name{default_model}
\alias{default_model}
\title{Construct A Default Severity Model}
\usage{
default_model(line_list, population)
}
\arguments{
\item{line_list}{A \code{data.frame} (or \code{data.frame} extending object like a
\code{tibble}) of line list data.}

\item{population}{A \code{data.frame} (or \code{data.frame} extending object like a
\code{tibble}) of population data.}
}
\value{
A \linkS4class{SeverityEstimateModel} S4 object instance.
}
\description{
Construct a \linkS4class{SeverityEstimateModel} from already-formatted line
list and population data by inferring the model structure from column names.

The \code{line_list} must contain \code{time}, \code{detection}, and \code{outcome} columns.
Every other \code{line_list} column is treated as a strata column with
\code{degrees_of_freedom = 0L}, so those columns must also be present in
\code{population}. The \code{population} data must then contain exactly one additional
non-strata column, which is treated as the population count column.

Detection values must be case-insensitive forms of \code{active}/\code{passive} or
\code{a}/\code{p}. Outcome values must be case-insensitive forms of
\code{asymptomatic}/\code{symptomatic}/\code{death} or \code{a}/\code{s}/\code{d}.

The returned model includes weakly informative detection priors suitable for
fitting immediately with \code{\link[=fit]{fit()}}.
}
\examples{
line_list <- data.frame(
  time = c(1L, 1L, 2L),
  age = c("Youth", "Adult", "Senior"),
  detection = c("Active", "Passive", "Active"),
  outcome = c("Asymptomatic", "Death", "Symptomatic")
)
population <- data.frame(
  age = c("Youth", "Adult", "Senior"),
  value = c(1000L, 1200L, 900L)
)
model <- default_model(line_list, population)
model

}
